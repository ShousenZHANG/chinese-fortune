# v4 实际回答评测：提前结束记录

**2026-09-08，用户明确要求快速结束并发布最新版本，因此停止剩余录制与评审。完整的新旧版本比较没有完成，本报告不提供整体提升百分比、全版本准确率或现实预测准确率。**

已启动的会话自然收尾；最终没有在途模型调用，也没有本轮被强行截断的回答。停止原因是 `user_requested_stop`，不是内容质量失败。详细数量、用量口径与原始记录哈希见 [机器摘要](recording-summary.json)。

## 受测版本与冻结范围

| 项目 | 固定值 |
|---|---|
| 基线提交 | `524dc1739778017e0fd2c25dc19a7594a87d662c` |
| 本次新版受测提交 | `bffeae84f61f66b6e57382a54239132e866ca18d` |
| 新版运行输入 fingerprint | `9502eab1142cb5ebfca9bbcecf6a6348c5b4dcb0a6990d2f035216db041a194b`，509 个运行输入文件 |
| 模型 / 推理设置 | `gpt-6-astra` / `ultra`，实际宿主返回已核对 |
| 宿主 | Codex CLI `0.153.4`；独立 ephemeral、read-only 会话 |

最终维护报告的提交可能晚于受测提交；发布前须逐路径核对上述运行输入哈希。`evals/v4/` 和 `docs/MODEL-COMPARISON-PROCESS.md` 不进入运行包。此前 `fc5c255` 的录制作为历史资料保留，不能改名算作新版 `bffeae8` 的结果。

原计划包括 24 道能力题与 8 道独立挑战题，每版本各两次，共 128 个会话。能力题包含 12 个完整八字、4 个真实多轮追问、4 个紫微、4 个六爻；多轮题按同一会话顺序交互。题目没有因为结果而修改，原 30 题保留作历史材料。

## 截止数量

| 版本 | 能力集完整会话 | 挑战集完整会话 | 合计 | 尚未完成的计划槽位 |
|---|---:|---:|---:|---:|
| 基线 | 24 / 48 | 10 / 16 | 34 / 64 | 30 个待补录 |
| 新版 `bffeae8` | 8 / 48 | 0 / 16 | 8 / 64 | 56 个未启动 |

这 42 个“完整会话”仅指执行完成，**不等于 42 条回答已经通过内容评审**。

- 9 月 6 日的基线已有 23 个完整会话；9 月 8 日补录 11 个，全部执行成功。基线第一轮 32 题已齐，第二轮仅完成挑战 C01、C02。
- 新版完成 B01–B08 的第一次会话，共 8 个；尚未完成新版多轮、紫微、六爻与挑战组的正式回答录制。
- 9 月 8 日共执行并完成 19 个会话，无新增执行失败，无在途或中途截断会话。最后会话于 `2026-09-08T02:27:28Z` 收尾。
- 旧实验 93 条 `usageLimitExceeded` 原始失败记录全部保留，其中基线 41 条、已被替代的 `fc5c255` 实验 52 条。它们是账户额度失败，不能统计为内容错误；补录也没有覆盖首个失败。

最终只有 B01–B08 共 8 题拥有两版本的完整原始回答。完整重复实验没有做完，不据此外推其他方法或整体收益。

## 已完成的内容评审

按照 [实际比较流程](../../docs/MODEL-COMPARISON-PROCESS.md)，公开包隐藏版本元数据并随机排序；事实与可读性由两个评审角色分别判断，按原始答文、工具轨迹及公开包哈希绑定后合并。事实轴检查盘面、引文、条件、范围、完成度和纠错诚实度；表达轴检查可读性、相关性，并用自己的话复述结论、依据和限制。没有把未审字段自动填写为通过。

| 本次比较可用的已审回答 | 数量 | 已观察结果 |
|---|---:|---|
| 基线 | 7 | 8 项判据均通过 |
| 新版 `bffeae8` | 4 | 8 项判据均通过 |
| 两边都完成双轴评审的题目 | B01–B04 | 4 对回答 |

这里的“未发现语义错误”**仅限上述已审样本**。此前另有 6 条 `fc5c255` 的已审回答，只保留在历史记录中，不加入新版数量。已经导出、但没有完成双轴合并的其他包不计入通过数。

这些是会话内、隐藏版本标签的评审。答案内容和功能差异仍可能暴露版本；不冒称外部专家鉴定或严格双盲实验。样本少、覆盖不完整，不能写成“全版本 100% 通过”。

## 实际调用与时间核查

已完成 7 条绑定原始哈希的操作侧车，尚未覆盖所有完整回答。统计区分宿主工具项、真正 CLI 进程、显式 Python 函数调用和程序内部计算；`--help`、读取源码、JSON 校验不算排盘。循环中的子进程须按可见控制流逐个计数。

| 题目 / 第一次会话 | 基线外部排盘次数 | 新版外部排盘次数 |
|---|---:|---:|
| B01 | 2：一次 CLI、一次显式 `calculate_bazi` | 1 |
| B02 | 2 | 2 |
| B03 | 未完成对应操作侧车 | 1 |
| B04 | 未完成对应操作侧车 | 1 |

**C4 的接口字段问题已修，但“所有宿主重复计算都消失”没有得到验证。**新版 B02 两次相同输入的 `bazi_reading` 均成功，返回字节完全相同的 JSON。记录没有证明原因；不能猜成输出被截断或仍缺契约字段。这是已观察到的效率缺口，不自动等同于排盘或引文错误。

上述新版 B01–B04 均先真实采样悉尼当前时间，再保持题目指定的重演 UTC，正文也区别了重演与实时。补录的基线 C08 是历史月建核查：没有观察到实时采样，但注入 UTC、两地同一瞬间和正文时间来源均已核对；其显式历法对照不能算作重新随机起卦。尚未核查的记录不填“全部取时正确”或“全部一次排盘”。

## 用量与边界

宿主用量采用每会话最后一次累计值；缓存输入包含在输入中，推理输出已经包含在输出中，不重复相加。9 月 8 日基线补录的已报告总量为 7,263,698 tokens，新版为 3,784,742 tokens；**两组题数和题型不同，这两个总数不能直接比较效率，也不是账单金额**。

首批基线在 9 月 6 日执行，补录及新版在 9 月 8 日执行；固定重演时刻始终为 `2026-09-06T03:15:00Z`。模型 ID、推理设置和题目受控，但缓存次序与服务端模型权重不可观测，不能假设后端完全没变。续跑工具只增加了重复编号起点与选题记录，原编号和首个失败保留；两个 runner 的哈希均记入机器摘要。

古籍转录符合性、传统规则解释和计算一致性，不等于现实预测有效性。工程测试、覆盖率和安装验证应分别报告，不能替代本次未完成的全量内容比较。原始回答、事件、首个失败、停止点和评审绑定保存在仓库外，路径与证据哈希记于机器摘要。
